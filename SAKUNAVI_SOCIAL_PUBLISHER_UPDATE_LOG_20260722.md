# Sakunavi Social Publisher 更新ログ（〜2026-07-22）

対象：`wp-content/plugins/sakunavi-social-publisher/`（新規プラグイン）

「サクッとお金ナビ WordPress SNS配信管理プラグイン 制作仕様書」（全43ページ）をもとに、実際のSNS投稿（X／Threads／Instagram／LINE）を除いた土台部分（Phase 1〜3）を実装。X/Meta/LINEのAPI認証情報が未取得のため、実配信機能（Phase 4以降）は今回のスコープ外。

---

## 1. プラグイン本体（Phase 1〜3）

WordPressプラグインとして新規作成。テーマの`functions.php`には一切手を加えず独立して動作する（テーマ変更時にも機能を維持できるようにするため）。

### 対象投稿タイプ
- `column`（お金コラム）
- `ranking`（ランキング）
- `ranking_variant`（ランキングのA/Bバリアント）※単独の公開URLを持たないため、SNS投稿のリンク先は親の`ranking`記事のURLを使用
- `card_loan_company`（カードローン会社）

### やったこと

**編集画面の「SNS配信」パネル**（仕様書9章）
- 上記4投稿タイプの編集画面下部にメタボックスとして追加（ブロックエディタ／クラシック編集画面のどちらでも同じ保存経路で動作）
- 配信種別・配信先（X/Threads/Instagram/LINE候補）・配信タイミング・共通URL（読み取り専用）・UTM自動付与・SNS用要約（文字数カウンター付き）・共通SNS画像・配信担当者／承認担当者・内部メモ・SNS配信対象外フラグ
- チャンネルごとのアコーディオン（X/Threads/Instagram/LINE）：本文・ハッシュタグ（X/Instagramのみ）・画像・文字数カウンター（X=280／Threads≈500／Instagram≈2200）
- 「テンプレートで生成」ボタン：入力済み（空でない）欄は自動上書きしない。「テンプレートで上書きする」ボタンでのみ強制上書き

**初回公開判定・リライト検知**（仕様書10〜11章）
- 記事を初めて公開したタイミングを自動検知（誤って「非公開→再公開」を初回公開と判定しないよう対策済み）
- 公開済み記事の保存時に、実質的な内容変更（見出し構成・本文・サムネイル・ACFの主要項目）だけを検知し、「この記事は以前SNSへ配信されています。今回の更新をSNSで告知する」チェックボックスを表示
- 誤字修正・カテゴリ変更・SNS文言だけの変更・下書き保存中の自動保存等は検知対象外（仕様書11-4の除外基準に準拠）
- `ranking`は約150個のACFフィールド（各順位のタイトル・金利・スピード・使いやすさ・ここがポイント・口コミ等）、`card_loan_company`は金利・限度額・審査時間・無利息期間等を検知対象に設定（CTAラベルやロゴ画像等の運用上の変更は対象外）
- リライト検知の感度（類似度しきい値）は管理画面から調整可能

**投稿テンプレート・プレビュー**（仕様書12〜16章）
- X／Threads／Instagram／LINEそれぞれに、初回公開用／リライト更新用／重要更新用のデフォルト文面をあらかじめ登録（仕様書のテンプレートをそのまま反映）
- 管理画面「投稿テンプレート」から文面・カテゴリー別ハッシュタグ・リライト検知の感度を編集可能
- 記事タイトル・URL・要約・更新概要などを`{article_title}`のような変数で自動差し込み

**金融メディア向け表現チェック**（仕様書25章）
- 「絶対に審査に通る」「必ず即日融資」等、誤解を招く表現をNGワードリストと照合し、プレビュー時に警告表示（投稿はブロックしない）
- NGワードリストは管理画面から編集可能

**管理画面「SNS配信」メニュー**
- 配信ダッシュボード（簡易版。SNS配信対象の記事数・未告知のリライト件数などを表示）
- リライト更新一覧（検知履歴の一覧・絞り込み）
- 投稿テンプレート
- 接続設定（X/Meta/LINEの認証情報入力欄。`wp-config.php`の定数を優先、値は画面上でマスク表示。**実際の接続テストはまだ行わない**）

**DB設計**
- 独自テーブルを3つ新設：`snp_revisions`（リライト更新履歴）／`snp_jobs`（将来の配信ジョブキュー。今は空のまま）／`snp_logs`（将来の実行ログ。今は空のまま）
- FTP等でファイルだけを更新した場合でもテーブル定義が追従するよう、無停止マイグレーションの仕組みを組み込み済み

### まだ実装していないこと（Phase 4以降）
- X／Threads／Instagram／LINEへの実際の投稿・接続テスト
- Cronによる予約配信・再試行
- 承認フローの強制（今は「告知する」チェックのみで、実配信の権限管理はまだ機能しない）
- 投稿履歴・エラーログ画面（配信が始まるまでは中身が空になるため、メニュー自体を非表示にしている）
- GA4連携・通知メール

### 触ったファイル
新規プラグイン一式（`wp-content/plugins/sakunavi-social-publisher/`）。主なファイルのみ抜粋：
- `sakunavi-social-publisher.php`（エントリーポイント）
- `includes/class-snp-post-types.php`（対象投稿タイプ・検知対象フィールドの定義）
- `includes/detection/class-snp-content-hasher.php`（リライト検知のロジック）
- `includes/admin/class-snp-edit-screen-panel.php`（編集画面のSNS配信パネル）
- `includes/templates/`（テンプレート・ハッシュタグ）
- `includes/compliance/class-snp-expression-checker.php`（NGワードチェック）
- `includes/admin/screens/`（管理画面4画面）

---

## 2. 不具合修正：テンプレート生成が保存前の入力を反映しない

### 症状
SNS用要約欄などに入力しても、「更新」で保存する前に「テンプレートで生成」を押すと、入力した内容が反映されなかった。

### 原因
「テンプレートで生成」ボタンは、記事IDだけをサーバーに送り、サーバー側がDB（保存済みの値）から文面を組み立てていたため、画面上でまだ保存していない入力内容は見えていなかった。

### 対応
ボタンを押した時点の画面上の入力値（SNS用要約・更新タイトル・更新概要・主な変更点）もAjaxリクエストに含めて送信し、サーバー側もその値を優先して使うように修正。保存前でも最新の入力内容がテンプレートに反映されるようになった。

### 触ったファイル
- `includes/templates/class-snp-template-engine.php`
- `includes/ajax/class-snp-ajax-handlers.php`
- `assets/js/snp-admin-panel.js`

---

## 3. 運用方針の決定：Phase 4（API自動投稿）は売上目標達成まで見送り

### 背景
X（旧Twitter）でAPI経由の投稿を行うには、開発者アプリの登録に加え、有料プランへの加入が必要（無料プランでは投稿がほぼできない）。Meta（Threads/Instagram）・LINEも含め、自動投稿には各SNS側のAPI利用コストが発生する。

### 決定事項
- **当面、SNSへの自動投稿（Phase 4）の実装・API登録は行わない。**
- 「SNS配信」パネルで文章・画像を準備するところまでは今まで通り運用し、実際の投稿は担当者が各SNSへ**手動でコピー＆投稿**する運用とする。
- **売上目標を達成した段階で、あらためてAPI連携（自動投稿）の導入を検討する。**
- この方針に合わせ、ライター向けマニュアルに「手動投稿の手順」セクションを追加し、「今後の対応で自動投稿になる」という曖昧な書き方から「当面は手動運用」という確定した書き方に修正した。

### 触ったファイル（マニュアル）
- `SAKUNAVI_SOCIAL_PUBLISHER_WRITER_MANUAL_20260722.md`（手動投稿の手順セクション追加、運用方針の文言を確定表現に修正）
- `SAKUNAVI_COLUMN_POSTING_MANUAL_20260714.md`（SNS配信セクションの注記を同じ方針に統一）

---

## 4. 接続設定画面：Xの認証情報欄を実際に必要な項目へ修正

### 内容
Xの投稿API（OAuth 1.0a）で実際に必要なのは「API Key／API Key Secret／Access Token／Access Token Secret」の4項目だが、接続設定画面には「Client ID／Client Secret／Access Token」の3項目しか用意していなかった。将来Phase 4に着手する際に迷わないよう、今のうちに正しい項目名・4項目構成に修正した（保存・API接続処理自体はPhase4未着手のため、今回は項目定義のみの修正）。

### 触ったファイル
- `includes/admin/screens/class-snp-screen-connections.php`

---

## 5. 各チャンネルに「コピー」「投稿画面を開く」ボタンを追加

### 背景
手動投稿の運用（3章）を採用したことに伴い、ライターの手間をできるだけ減らしたい、という要望。

### 対応
- 全チャンネル共通：「コピー」ボタンで、今入力中の文章（未保存分も含む）をクリップボードにコピー
- X／Threads：**公式のWeb Intent機能**を利用し、「投稿画面を開く」ボタンを押すと投稿文が入った状態でX／Threadsの投稿画面を新規タブで開ける（APIキー・課金不要）
- Instagram／LINE：事前入力の仕組みが公式に用意されていないため、「開く」ボタンはアプリ／LINE公式アカウントマネージャーを開くだけ（文章は「コピー」ボタンで別途コピーして手動貼り付け）

### 触ったファイル
- `includes/admin/class-snp-edit-screen-panel.php`
- `assets/js/snp-admin-panel.js`

---

## 動作確認済み
- プラグイン有効化時に致命的エラーが発生しないこと
- 編集画面下部にSNS配信パネルが表示され、入力内容が保存後も保持されること
- 保存前の入力がテンプレート生成に反映されること
