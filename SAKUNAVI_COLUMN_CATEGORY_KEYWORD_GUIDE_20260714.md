# お金コラム投稿：「コラムカテゴリ」「キーワード」の設定ガイド

お金コラムの記事を投稿・編集する際、右サイドの投稿設定パネルに「コラムカテゴリ」と「キーワード」という2つの項目があります。似ているようで役割が違うので、使い分け方をまとめます。

---

## この2つは何が違うのか

| | コラムカテゴリ | キーワード |
|---|---|---|
| 役割 | サイトの構造・分類（棚分け） | 記事の話題・トピック（検索用タグ） |
| 数の目安 | サイト全体で数個〜十数個、めったに増えない | 記事ごとに自由に増やせる |
| 1記事あたり | 1〜2個程度 | 2〜5個程度 |
| 表示される場所 | サイドバー「カテゴリー一覧」、パンくず、URL構造 | サイドバー・トップページの「人気のキーワード」 |
| 新しく追加する頻度 | 低い（サイト構成に関わるので慎重に） | 高い（記事ごとに気軽に追加してOK） |

一言でいうと、**カテゴリは「本棚のどの棚に置くか」、キーワードは「この記事にどんな付箋を貼るか」**というイメージです。

---

## 1. 「コラムカテゴリ」の設定

### 使い方
1. 投稿設定パネルの「コラムカテゴリ」を開く
2. 既存のカテゴリ一覧からチェックを入れる（複数選択も可能ですが、基本は1つ、多くても2つまでを推奨）
3. 該当するカテゴリがなければ「新規カテゴリを追加」から作成できますが、**カテゴリはサイトの棚分け（サイドバーの「カテゴリー一覧」やメニュー構造）に直結するため、新規作成は慎重に**。まず既存のカテゴリで代用できないか確認してください。

### 特殊なカテゴリ：「条件検索」配下
`conditional`（条件検索）というカテゴリの**子カテゴリ**（学生向け・主婦向けなど、条件で絞り込むためのもの）は、サイドバーの「カテゴリー一覧」には出さず、別枠の「条件一覧」として表示する仕組みになっています。「〇〇向け」「〇〇の条件」といった条件検索系の記事を書く場合は、この`conditional`の子カテゴリから選んでください。

---

## 2. 「キーワード」の設定

### 使い方
1. 投稿設定パネルの「キーワード」を開く
2. 記事の内容に関連する単語を入力してEnter（複数入力可）
3. 入力中に過去使ったキーワードが候補として表示されるので、**新しく入力する前に候補を確認し、同じ意味のキーワードが既にあればそちらを選ぶ**

### 良いキーワードの例
記事の中で読者が検索しそうな、具体的で短い単語・フレーズが向いています。

- ✅ 良い例：`即日融資`、`学生`、`在籍確認`、`おまとめローン`、`専業主婦`
- ❌ 避けたい例：`とても便利な借り入れの方法について`（長すぎる・文章になっている）

### 表記を必ず揃える
キーワードは使用回数が多い順に自動集計されて「人気のキーワード」に表示されます。表記がバラバラだと別々のキーワードとして扱われ、正しく集計されません。

- ✅「即日融資」で統一
- ❌ ある記事は「即日融資」、別の記事は「即日 融資」「即日ローン」…のようにバラバラ

迷ったら、投稿一覧画面の「キーワード」列（一覧に表示されています）で、他の記事がどんな表記を使っているか確認してから入力するのがおすすめです。

---

## 3. 入力の流れ（まとめ）

1. 記事を書き終えたら、右サイドの投稿設定パネルを開く
2. 「コラムカテゴリ」→ 該当する棚（カテゴリ）に1〜2個チェック
3. 「キーワード」→ 記事の内容を表す単語を2〜5個、既存の候補から選びながら入力
4. 公開・更新

### 記入例（学生向けの即日融資に関するコラムの場合）
- **コラムカテゴリ**：条件検索 > 学生向け（`conditional`の子カテゴリ）
- **キーワード**：`即日融資`、`学生`、`在籍確認`、`無利息期間`
