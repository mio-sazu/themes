# サクナビ 投稿の指南書

「カードローン会社」「お金コラム」を投稿・編集する方向けのガイドです。返済シミュレーター・カードローン診断・人気のキーワード機能が正しく動くために、入力時に気をつけていただきたい点をまとめています。

---

## 1. カードローン会社（`card_loan_company`）投稿の基本

### 1-1. 「実質年率（最大）」は必ず入力する

`rate_max`（会社情報タブの「金利（最大）」）が空欄の会社は、**返済シミュレーターにもカードローン診断にも一切表示されません**。会社を新規登録したら、まずここを埋めてください。

### 1-2. 「表示する／表示しない」トグルはすべて“デフォルトON”

返済シミュレーターの会社選択・カードローン診断の候補、どちらも以下の考え方で統一しています。

- 何も設定しなければ → **表示される**
- 明示的に「表示しない」を選んだ場合のみ → 非表示

| 機能 | サイドバーの項目名 | 初期状態 |
|---|---|---|
| 返済シミュレーター | 返済シミュレーターに表示する（`sim_show`） | 表示する |
| カードローン診断 | 診断結果に表示する（`diag_show`） | 表示する |

新しく会社を追加したときに「なぜか出てこない」場合は、どちらかが誤って「表示しない」になっていないか確認してください。

### 1-3. おすすめ表示（`sim_featured`）

返済シミュレーターの会社選択で、大きなカードで目立たせたい会社だけONにしてください（こちらは今まで通りデフォルトOFFです）。OFFの会社は「その他の会社から選ぶ」の検索一覧に入ります。

---

## 2. カードローン診断（トップページ「カードローンを探す」）専用の項目

編集画面右側の「カードローン診断 表示設定」に、以下の項目があります。**実際の各社の公式サイト・規約で確認できる範囲だけをONにしてください。** 事実と異なる状態でONにすると、そのまま利用者への誤案内になります（特に在籍確認・専業主婦の利用可否は貸金業の表示として重要です）。

| 項目 | 意味 |
|---|---|
| 学生の利用 | 学生の申込みを受け付けているか |
| パート・アルバイトの利用 | パート・アルバイトの申込みを受け付けているか |
| 即日融資 | 即日での融資に対応しているか |
| 原則、在籍確認なし | 原則として在籍確認なしで案内できるか |
| 専業主婦（夫）の利用 | 本人に収入がない配偶者でも申込み可能か（配偶者貸付など） |
| アプリ完結・カードレス対応 | スマホアプリのみで完結し、カードレスで利用できるか |
| おまとめ・借り換え対応 | おまとめローン・借り換えに対応しているか |
| 土日・夜間の借入対応 | 土日・夜間でも振込やATM等での借入に対応できるか |

### 2-1. 「低金利重視」「高額融資に対応」は入力欄がなく自動判定

この2つの条件だけは専用の入力欄を作らず、既存の数値から自動的に判定しています。

- **低金利重視**：「金利（最小）」（`rate_min`）が **4.5%以下** の会社をマッチ扱い
- **高額融資に対応**：「融資限度額（最大）」（`limit_amount_max`）が **500万円以上** の会社をマッチ扱い

この2項目だけを個別に狙って調整することはできません。実際の`rate_min`・`limit_amount_max`の数値が正しければ、自動的に正しく判定されます。

### 2-2. マッチ度がどの会社も同じように見える場合

「即日融資」「在籍確認なし」などの新しい条件は、**入力されていない会社はすべて“非対応”扱い**になります。ある条件について1社もONにしていない状態でその条件を選ぶと、全社が同じスコア（0点）になり、結果としてマッチ度が横並びに見えてしまいます。

→ 対処法：各社の編集画面で、上記の表の項目を実際の情報にもとづいて埋めてください。データが揃うほど、会社ごとの差がはっきり出るようになります。

---

## 3. お金コラム（`column`）投稿時の注意点

### 3-1. キーワードの表記を揃える

編集画面の「キーワード」欄に入力したタグは、使用回数が多い順にトップページ・サイドバーの「人気のキーワード」に自動表示されます。

- 「即日融資」と「即日 融資」のように表記がバラバラだと、別のキーワードとして扱われ、集計が正しく積み上がりません。
- 入力中に過去のキーワードが候補として出てくるので、なるべくそこから選んでください。
- 1件もキーワードが登録されていない間は、「人気のキーワード」エリアごと非表示になります（表示されなくても異常ではありません）。

---

## 4. 困ったときのチェックリスト

**会社が返済シミュレーター／カードローン診断に出てこない**
- [ ] `rate_max`（実質年率の最大）は入力済みか
- [ ] `sim_show` / `diag_show` が誤って「表示しない」になっていないか
- [ ] 投稿ステータスが「公開」になっているか

**カードローン診断のマッチ度に差が出ない**
- [ ] 選んだ「こだわり条件」のうち、実際に会社ごとにON/OFFが入力されている項目はあるか
- [ ] 「無利息期間を重視」「WEB完結希望」「低金利重視」「高額融資に対応」など、既存データを使う条件だけで試すと差が出るか

**人気のキーワードが表示されない**
- [ ] コラム記事に1件以上キーワードが登録されているか
- [ ] キーワードの表記が記事ごとにバラバラになっていないか
