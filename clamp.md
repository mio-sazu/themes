# clamp()について

`clamp()` は、CSSで文字サイズや余白などを **最小値・推奨値・最大値** の3つで指定できる関数です。

レスポンシブデザインでは、スマートフォン・タブレット・PCなど、画面幅に応じて文字サイズや余白を自然に変化させたい場合に使用します。

---

## 1. clamp()の基本構文

```css
font-size: clamp(最小値, 推奨値, 最大値);
```

### 例

```css
h1 {
  font-size: clamp(28px, 5vw, 48px);
}
```

この場合、H1の文字サイズは以下のように変化します。

| 指定 | 意味 |
|---|---|
| `28px` | これ以上小さくならない最小サイズ |
| `5vw` | 画面幅に応じて変化する推奨サイズ |
| `48px` | これ以上大きくならない最大サイズ |

つまり、スマートフォンでは **28px程度**、PCでは **最大48pxまで** 自然に大きくなります。

---

## 2. clamp()を使うメリット

`clamp()` を使うことで、デバイスごとに細かくメディアクエリを書かなくても、画面幅に応じた自然なサイズ調整ができます。

### 主なメリット

| メリット | 内容 |
|---|---|
| 自然に可変できる | 画面幅に合わせて文字サイズや余白を調整できる |
| メディアクエリを減らせる | SP / PCごとの細かい指定を減らせる |
| スマホで大きすぎない | 小さい画面で見出しが大きくなりすぎるのを防げる |
| PCで小さすぎない | 大きい画面で文字が小さく見えるのを防げる |
| デザインがなめらかになる | デバイス幅に応じて自然に変化する |

---

## 3. 見出しでの使用例

```css
h1 {
  font-size: clamp(28px, 5vw, 48px);
  line-height: 1.3;
}

h2 {
  font-size: clamp(22px, 4vw, 32px);
  line-height: 1.5;
}

h3 {
  font-size: clamp(18px, 3vw, 24px);
  line-height: 1.6;
}
```

### 見出しサイズの考え方

| 要素 | 最小値 | 推奨値 | 最大値 | 用途 |
|---|---:|---:|---:|---|
| H1 | 28px | 5vw | 48px | ページタイトル・ヒーロー見出し |
| H2 | 22px | 4vw | 32px | セクション見出し |
| H3 | 18px | 3vw | 24px | 小見出し |

---

## 4. 本文では使いすぎない

`clamp()` は見出しや大きなテキストには便利ですが、本文に使う場合は注意が必要です。

本文サイズが画面幅によって変化しすぎると、読み心地が不安定になることがあります。

### 本文の基本ルール

```css
body {
  font-size: 16px;
  line-height: 1.8;
}
```

本文は原則として **16px以上** を維持します。

必要に応じてPC幅で少し大きくする程度なら、以下のように設定できます。

```css
body {
  font-size: clamp(16px, 1.2vw, 18px);
}
```

---

## 5. clamp()を使うとよい箇所

| 用途 | 使用可否 | 理由 |
|---|---|---|
| H1 | 推奨 | 画面幅に応じて大きく見せたい |
| H2 | 推奨 | SPとPCで自然に差をつけやすい |
| H3 | 使用可 | 小見出しにも適用できる |
| 本文 | 控えめに使用 | 可変しすぎると読みづらくなる |
| ボタン文字 | 基本は固定推奨 | 操作性を安定させるため |
| 余白 | 使用可 | セクション余白などに向いている |
| アイコンサイズ | 基本は固定推奨 | 見た目と操作領域を安定させるため |

---

## 6. 余白での使用例

`clamp()` は文字サイズだけでなく、セクションの余白にも使用できます。

```css
.section {
  padding-block: clamp(40px, 8vw, 96px);
}
```

この場合、セクションの上下余白は以下のように変化します。

| 画面幅 | 余白 |
|---|---:|
| スマートフォン | 最小40px |
| タブレット | 画面幅に応じて可変 |
| PC | 最大96px |

スマートフォンでは間延びせず、PCではゆったりしたレイアウトを作ることができます。

---

## 7. clamp()使用時の注意点

| 注意点 | 内容 |
|---|---|
| 最小値を小さくしすぎない | スマホで読みにくくなるため |
| 最大値を大きくしすぎない | PCで見出しが大きくなりすぎるため |
| 本文の可変幅は控えめにする | 読み心地を安定させるため |
| `vw` の値を強くしすぎない | 画面幅による変化が大きくなりすぎるため |
| UIパーツは操作性を優先する | ボタンやフォームは安定したサイズが望ましい |

---

## 8. 推奨ルール

本設計では、`clamp()` は主に以下の用途で使用します。

| 使用する場面 | 推奨度 |
|---|---|
| H1 / H2などの見出しサイズ | 高 |
| ヒーローエリアのキャッチコピー | 高 |
| セクション上下余白 | 高 |
| LPの大きな訴求テキスト | 中〜高 |
| PCとSPで自然に差をつけたい要素 | 中 |
| 本文 | 低〜中 |
| ボタン・ナビゲーション・フォーム | 低 |

---

## 9. 実務でのおすすめ指定

### H1

```css
font-size: clamp(28px, 5vw, 48px);
```

### H2

```css
font-size: clamp(22px, 4vw, 32px);
```

### H3

```css
font-size: clamp(18px, 3vw, 24px);
```

### 本文

```css
font-size: 16px;
```

または

```css
font-size: clamp(16px, 1.2vw, 18px);
```

### セクション余白

```css
padding-block: clamp(40px, 8vw, 96px);
```

---

## 10. まとめ

`clamp()` は、画面幅に応じて文字サイズや余白を自然に変化させるための便利なCSS関数です。

特に、以下のような場面で効果的です。

- 見出しサイズをスマホとPCで自然に変えたい
- セクション余白を画面幅に合わせて調整したい
- メディアクエリを増やしすぎずにレスポンシブ対応したい
- デザインに柔らかい可変性を持たせたい

一方で、本文・ボタン・フォームなど、読みやすさや操作性を安定させたい要素では、可変させすぎないように注意します。

基本的には、**見出し・ヒーローコピー・セクション余白に使う** と覚えておくと、安全に運用できます。