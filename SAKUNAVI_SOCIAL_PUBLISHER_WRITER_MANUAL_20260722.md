# SNS配信パネル 使い方マニュアル（ライター向け）

記事を作成・更新するときに、WordPress編集画面の下の方に表示される「SNS配信」というボックスの使い方について説明します。

> **今できること／まだできないこと**
> 現在このパネルは「SNS投稿用の文章を準備する」ところまでです。ここに入力・保存しても、**X・Threads・Instagram・LINEへ自動で投稿されることはありません。** 自動投稿にはSNS側の有料API利用が必要になるため、**当面はこのパネルで準備した文章・画像を、担当者が各SNSへ手動でコピー＆投稿する運用**とすることが決まっています（売上目標を達成した段階で、あらためて自動投稿（API連携）の導入を検討します）。「入力したのに投稿されない」は不具合ではなく、今の運用方針です。

---

## 1. どこにあるの？

対象記事（お金コラム／ランキング／カードローン会社）の編集画面で、本文エディタの下までスクロールすると「SNS配信」というボックスがあります。

---

## 2. 新しい記事を書いたとき（初回公開）

1. 記事タイトル・本文を書き終えたら、SNS配信ボックスの「配信種別」で **初回公開** を選ぶ（多くの場合すでに選ばれています）
2. 「配信先」で、投稿したいSNSにチェック（X／Threads／Instagram／LINE配信候補へ追加）
3. 「SNS用要約」欄に、記事の内容を80〜150文字程度でまとめて入力
   - 文字数はリアルタイムでカウントされます
4. （任意）「共通SNS画像」でSNS投稿用の画像を選択。選ばなければ各チャンネルの入力欄で個別に選べます
5. チャンネルごと（X／Threads／Instagram／LINE）の欄を開き、「テンプレートで生成」ボタンを押す
   - 記事タイトル・URL・SNS用要約などから自動で文章の下書きが作られます
   - Instagram・LINEは画像が別途必要です（下の欄で選択）
6. 生成された文章を読んで、SNSごとに合わせて手直しする（そのままでも構いません）
7. 最後に画面右上の「更新」または「公開」ボタンを押して保存

> **文字数の目安**：X＝280文字まで／Threads＝約500文字まで／Instagram＝約2,200文字まで。それぞれの入力欄の下に文字数が表示されます。

---

## 3. 公開済みの記事を大きく直したとき（リライト更新）

以前公開してSNSにも案内したことがある記事の内容を大きく直すと、次に保存したタイミングで自動的に「この記事は以前SNSへ配信されています」という欄が表示されます。

> **表示される条件**：見出しの追加・本文の大幅な書き換え・アイキャッチ画像の変更・金利や限度額など主要な情報の変更など、**読者に知らせる価値のある変更**をしたときだけです。誤字修正や句読点の調整、カテゴリーの変更だけでは表示されません。

表示されたら、以下を入力します。

1. 「今回の更新をSNSで告知する」にチェック
2. 「更新タイトル」「更新概要」を入力（何がどう変わったかを一言で）
3. 「主な変更点」を1〜3個、箇条書きで入力
4. 「重要度」を選ぶ（軽微／通常／重要／緊急）
   - 誤字修正レベルの軽微な更新はSNS告知しないのが基本方針です
5. 各チャンネルの「テンプレートで生成」ボタンを押すと、リライト用の文面（「記事を更新しました」等）が自動生成されます
6. 内容を確認・調整して保存

> **ポイント**：「更新タイトル」「更新概要」「主な変更点」は、まだ保存していない状態でも「テンプレートで生成」を押した時点の入力内容がそのまま文章に反映されます。先に更新概要などを埋めてから生成ボタンを押すのがおすすめです。

---

## 4. 「テンプレートで生成」と「テンプレートで上書きする」の違い

- **テンプレートで生成**：まだ何も書いていない欄だけを自動で埋めます。すでに自分で書いた文章がある欄は上書きされません（変わらなかった場合「既存の入力を保持しました」と表示されます）
- **テンプレートで上書きする**：今書いてある内容を破棄して、テンプレートの内容で丸ごと書き直します。押すと確認メッセージが出ます

自分で手直しした文章を消したくないときは「テンプレートで生成」だけを使ってください。

---

## 5. 手動投稿の手順（当面の運用）

各チャンネルの入力欄の下に「コピー」ボタンと「投稿画面を開く」ボタンが追加されました。

- **X／Threads**：「◯◯の投稿画面を開く」ボタンを押すと、**今入力している文章がすでに入った状態**でX・Threadsの投稿画面が新しいタブで開きます。内容を確認して、画像があれば添付し、そのまま投稿できます（コピー＆貼り付けの手間なし）
- **Instagram／LINE**：文章を自動で入れる仕組みが公式に用意されていないため、ボタンを押すとアプリ・管理画面が開くだけです。「コピー」ボタンで文章をコピーしてから、開いた画面に手動で貼り付けてください

手順まとめ：

1. WordPressの編集画面で、投稿したいチャンネルの文章を確認・調整する
2. 画像を使う場合は、「画像を選択」欄に設定した画像をクリック→WordPressのメディア詳細画面などから元画像を**ダウンロード**する
3. 「コピー」または「投稿画面を開く」ボタンを押す
   - X／Threads：開いた投稿画面に文章が入っているので、画像を添付して投稿するだけ
   - Instagram：ダウンロードした画像をInstagramアプリにアップロードし、キャプション欄に「コピー」した文章を貼り付ける
   - LINE：LINE公式アカウントマネージャーでメッセージを作成し、「コピー」した文章・画像を貼り付けて配信する
4. 各SNSアプリ・サイト上で投稿を実行する
5. 投稿できたら、WordPress側の該当記事は特に操作不要です（投稿済みかどうかを記録する機能は今はまだありません）

> 💡 複数記事をまとめて投稿する日（LINEの週次まとめ配信など）は、対象記事を先に一覧でチェックしてから、まとめて文章を準備しておくと効率的です。

---

## 6. 気をつけたい表現について

投稿文の内容によっては、「絶対に審査に通る」「必ず即日融資」のような、金融広告として誤解を招きやすい表現が自動でチェックされ、該当すると警告が表示されます（投稿や保存自体はブロックされません）。警告が出た場合は、できるだけ表現を見直してください。

---

## 7. よくある質問

**Q. SNS配信ボックスに入力して保存したのに、SNSに投稿されません。**
A. 自動投稿機能は現在意図的に導入していません（当面は手動投稿の運用方針のため）。ここで用意した文章は、担当者が手動で各SNSに投稿します（「5. 手動投稿の手順」を参照）。

**Q. 「テンプレートで生成」を押したのに、SNS用要約欄の内容が反映されません。**
A. 以前あった不具合で、修正済みです。反映されない場合はページを再読み込みしてから試してください。

**Q. ランキングのバリアント記事（A/Bテスト用）にもSNS配信欄がありますが、リンク先はどこになりますか？**
A. バリアント記事自体には公開ページがないため、親のランキング記事のURLが自動的に使われます。

**Q. リライトしたのに「この記事は以前SNSへ配信されています」の欄が出てきません。**
A. 誤字修正など軽微な変更とみなされた可能性があります。見出しの追加や金利・限度額などの重要情報を変更した場合に表示される仕組みです。心当たりがあるのに出ない場合は担当者へ連絡してください。

**Q. 「配信先」で選んだSNSは、選んだだけで投稿されますか？**
A. されません。将来、自動投稿（API連携）を導入した際にどのSNSへ配信するかの設定として使われる項目です。今は「どのSNSに手動投稿する予定か」の目印として選んでおくだけで大丈夫です。
