# Webデザイン設計書：包括的サイズ・空間設計ルールセット

本設計書は、マルチデバイス対応、Webアクセシビリティ要件（WCAG 2.1 / 2.2）、および保守性の向上を目的とした、Webデザインにおけるサイズ感とレイアウトの標準仕様を定義するものです。

---

## 1. タイポグラフィ仕様

すべてのレイアウトの基点となるテキストサイズのルールです。  
アクセシビリティと人間工学に基づき設計します。

---

### 1.1 基本フォントサイズと階層

ブラウザデフォルトおよびアクセシビリティの観点から、本文の最小サイズは **16px** を厳守します。

| 要素 | 推奨サイズ | 設計の意図・ルール |
|---|---:|---|
| H1（大見出し） | 32px〜48px以上 | 流体タイポグラフィ（`clamp()`）を使用し、画面幅に応じて可変させる |
| H2（中見出し） | 24px以上 | 本文と明確なコントラストをつけるため、本文の1.5倍以上を目安とする |
| H3（小見出し） | 20px以上 | H2と本文の中間的な役割として使用する |
| Body（本文） | 16px以上 | **必須。** アクセシビリティ基準値。16px未満は眼精疲労やiOSでの意図しないズームを引き起こすため避ける |
| Button / Nav | 14px〜16px以上 | タップ領域との兼ね合いで調整し、視認性を確保する |
| Caption（注釈） | 12px〜14px | 補足情報に限定する。重要情報には使用しない |

---

### 1.2 余白（行間・字送り・段落間）

日本語特有の「仮想ボディ」を考慮し、欧文とは異なる独自のルールを適用します。

| 項目 | 推奨値 | ルール |
|---|---:|---|
| 行間（Line-Height） | 1.7〜1.9 | 16pxの場合、約28px相当。`1.2` など詰まりすぎた行間は、視線の折り返しを阻害するため避ける |
| 字送り（Letter-Spacing） | 0.04em〜0.08em | 日本語本文では微調整程度に留める。過度なトラッキングは避ける |
| 段落間（Margin-Bottom） | フォントサイズの2倍以上 | 例：本文16pxの場合は32px以上。意味のまとまりを視覚化する |

---

## 2. スペーシング・余白設計

レイアウトの秩序を保つため、すべての余白および要素サイズに **8pxグリッドシステム** を採用します。

---

### 2.1 スペーシングスケール

等差数列ではなく、コントラストを生むための特定の倍率スケールを使用します。  
Tailwind CSS準拠の考え方をベースとし、中途半端な値はできるだけ避けます。

| トークン名 | サイズ | 用途のガイドライン |
|---|---:|---|
| `space-1` | 4px | 例外値。アイコンとテキストの隙間など、微小な間隔 |
| `space-2` | 8px | 基準単位。基本パディング、密接な関連情報のグループ化 |
| `space-3` | 12px | 例外値。バッジやチップの微調整用 |
| `space-4` | 16px | 標準的なコンポーネント間、段落間、ボタン内部 |
| `space-6` | 24px | カード外側のマージン、グリッドのガター |
| `space-8` | 32px | ページマージン、主要セクション間の区切り |
| `space-12` | 48px | 視覚的なブレイクポイント、タップターゲット推奨値 |
| `space-16` | 64px | 主要なレイアウト分割の最大余白 |

---

### 2.2 内部余白（Padding）と外部余白（Margin）

#### Padding

コンポーネントの以下を形成します。

- 物理的なボリューム感
- タップ領域
- 視認性
- 押しやすさ

#### Margin

コンポーネント間の以下を定義します。

- 関係性
- 近接の原則
- 情報のまとまり
- 視覚的な区切り

#### 実装ルール

リストやカードの集合体には、要素ごとの `margin` ではなく、親コンテナに `gap` を使用することを推奨します。

```css
.card-list {
  display: grid;
  gap: 24px;
}
```

---

## 3. コンポーネント寸法仕様

コンポーネント自体の物理的な幅・高さにも8pxの倍数を適用し、アライメントを整えます。

---

### 3.1 ボタン

| 種類 | 高さ | 左右Padding |
|---|---:|---:|
| 標準サイズ | 48px | 32px |
| 小型サイズ | 32px | 16px〜24px |

#### ルール

- 主要CTAボタンは高さ48pxを基本とする
- スマートフォンではタップしやすさを優先する
- 見た目が小さいボタンでも、クリック領域は十分に確保する

---

### 3.2 カード

| 項目 | 推奨値 |
|---|---:|
| 内部Padding | 8px または 16px |
| 要素間Margin | 16px |
| 外側Margin / Gap | 24px |

#### ルール

- カード内部の情報は近接させる
- カード同士は24px以上空け、別要素であることを明確にする
- 一覧表示では `margin` よりも `gap` の使用を推奨する

---

### 3.3 アイコン

| 項目 | 推奨値 |
|---|---:|
| バウンディングボックス | 24px × 24px |
| タップ領域用Padding | 上下左右8px |

#### ルール

- アイコン自体は24px四方を基本とする
- タップ可能なアイコンは、透明なPaddingでクリック領域を拡張する
- 見た目のサイズと操作領域を分けて考える

---

## 4. レイアウト・コンテナ設計

人間の認知限界を考慮し、画面幅が広がってもコンテンツ幅は制限します。  
特に本文コンテンツでは、1行あたり **45〜75文字程度** を目安にします。

---

### 4.1 コンテナの最大幅

| 用途 | 推奨コンテナ幅 | ルール・意図 |
|---|---:|---|
| サイト全体最大幅 | 1140px〜1280px | 左右マージンを残し、視界の中心に収める |
| LP / 誘導枠 | 960px〜1140px | 視覚的誘導を目的としたマルチカラムとテキストのバランス用 |
| テキスト記事主体 | 640px〜760px | **重要。** 1行の長さを45〜75文字程度に保ち、眼精疲労を防ぐ |
| フルブリード | 1440px〜1920px | ヒーローバナーなど、視覚インパクト用途のみ制限を解除する |

---

### 4.2 ブレイクポイントとモバイル基準

モバイルファーストを前提とし、最小画面幅から順にレイアウトを拡張していく設計とします。

#### 最低対応幅

- 最低対応幅：**320px**
- 320px未満は基本的に想定しない
- ただし、320px幅では横スクロールやレイアウト崩壊が起きないようにする
- 画像・表・ボタン・カード・ナビゲーションが画面幅をはみ出さないようにする

---

### 4.3 ブレイクポイント設計

| 幅 | 想定デバイス / レイアウト | 主な設計方針 |
|---:|---|---|
| 〜480px | スマートフォン向け | 1カラム。余白は控えめにし、縦スクロールで読みやすくする |
| 481px〜767px | 大きめスマートフォン | 1カラムを基本としつつ、カードやボタンの余白を少し広げる |
| 768px〜1023px | タブレット縦向き | 1〜2カラム。必要に応じてカードグリッドを展開する |
| 1024px〜1279px | タブレット横向き / 小型PC | サイドバーや2〜3カラムレイアウトを開始する |
| 1280px〜 | デスクトップ大画面 | 最大コンテナ幅を設定し、横に広がりすぎないようにする |

---

### 4.4 モバイルファースト設計ルール

CSSは原則として、スマートフォン幅を基準に記述し、画面幅が広がるごとにメディアクエリで拡張します。

#### 基本方針

```css
/* まずモバイル幅を基準に書く */
.snv-c-card {
  padding: 16px;
}

/* 768px以上で拡張 */
@media (min-width: 768px) {
  .snv-c-card {
    padding: 24px;
  }
}

/* 1024px以上でさらに拡張 */
@media (min-width: 1024px) {
  .snv-c-card {
    padding: 32px;
  }
}
```

#### ルール

- モバイルを基準に設計する
- PC用CSSを先に書いて、あとからスマホで打ち消す設計は避ける
- メディアクエリは `min-width` を基本とする
- 320px幅で崩れないことを確認する
- 768px / 1024px / 1280pxでレイアウトの切り替えを確認する

---

### 4.5 モバイル時の余白ルール

モバイルでは画面幅が限られるため、PCと同じ余白をそのまま使わず、読みやすさと情報密度のバランスを取ります。

| 用途 | モバイル推奨 | タブレット推奨 | PC推奨 |
|---|---:|---:|---:|
| ページ左右余白 | 16px | 24px | 32px〜40px |
| セクション上下余白 | 40px〜56px | 56px〜72px | 72px〜96px |
| カード内部余白 | 16px | 20px〜24px | 24px〜32px |
| カード間余白 | 16px〜24px | 24px | 24px〜32px |
| 見出し下余白 | 16px〜24px | 24px | 24px〜32px |
| 本文段落間 | 24px〜32px | 32px | 32px〜40px |

#### 注意点

- モバイルで余白を詰めすぎると、読みづらくなる
- 逆に余白を広げすぎると、スクロール量が増えすぎる
- セクション間はしっかり空け、情報の区切りを明確にする
- カード内部の余白とカード同士の余白を混同しない

---

### 4.6 モバイル時の文字サイズルール

モバイルでも本文サイズは **16px以上** を維持します。  
小さすぎる文字は視認性を下げるため、重要情報には使用しません。

| 要素 | モバイル推奨 | PC推奨 |
|---|---:|---:|
| H1 | 28px〜36px | 36px〜48px |
| H2 | 22px〜28px | 24px〜32px |
| H3 | 18px〜22px | 20px〜24px |
| Body | 16px | 16px〜18px |
| Button | 15px〜16px | 16px |
| Caption | 12px〜14px | 12px〜14px |

#### ルール

- 本文は16px未満にしない
- 見出しは `clamp()` を使い、画面幅に応じて自然に可変させる
- キャプションは補足情報に限定する
- 重要な説明文を12px〜13pxにしない

#### 実装例

```css
.snv-c-heading-primary {
  font-size: clamp(28px, 6vw, 48px);
  line-height: 1.3;
}

.snv-c-heading-secondary {
  font-size: clamp(22px, 4vw, 32px);
  line-height: 1.5;
}
```

---

### 4.7 モバイル時のレイアウトルール

モバイルでは1カラムを基本とし、横並びレイアウトは原則として縦積みに変更します。

#### 基本ルール

- モバイルは1カラムを基本とする
- 横並びカードは縦並びにする
- サイドバーはメインコンテンツの下に配置する
- 表は横スクロールまたはカード型表示に切り替える
- ナビゲーションはハンバーガーメニューやドロワーにする
- CTAボタンは押しやすい幅・高さにする

#### 例：PCでは2カラム、モバイルでは1カラム

```css
.snv-l-two-column {
  display: grid;
  gap: 24px;
}

@media (min-width: 1024px) {
  .snv-l-two-column {
    grid-template-columns: minmax(0, 1fr) 300px;
    gap: 32px;
  }
}
```

---

### 4.8 モバイル時のタップターゲット

スマートフォンでは、見た目のサイズだけでなく、実際に指で押せる領域を十分に確保します。

| 項目 | サイズ |
|---|---:|
| 絶対下限値 | 24px × 24px |
| 実務上の推奨値 | 44px × 44px |
| 主要CTA | 48px以上 |
| アイコンボタン | 見た目24px + クリック領域44px以上 |

#### ルール

- 主要ボタンは高さ48px以上を基本とする
- テキストリンク同士を近づけすぎない
- アイコンが小さくても、透明なpaddingでタップ領域を広げる
- チェックボックスやラジオボタンも押しやすい余白を確保する

```css
.snv-c-icon-button {
  width: 44px;
  height: 44px;
  padding: 10px;
}

.snv-c-btn {
  min-height: 48px;
}
```

---

### 4.9 モバイル時の画像・表・埋め込み要素

モバイルでは、画像・表・動画・iframeが画面幅を超えないように制御します。

#### 画像

```css
img {
  max-width: 100%;
  height: auto;
}
```

#### 表

```css
.snv-c-table-scroll {
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}
```

#### iframe / 動画

```css
.snv-c-responsive-embed {
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 9;
}

.snv-c-responsive-embed iframe {
  width: 100%;
  height: 100%;
}
```

#### ルール

- 画像に固定幅を指定しない
- 表は必要に応じて横スクロール対応にする
- YouTubeやGoogle Mapなどの埋め込みはレスポンシブ対応にする
- SP時に横スクロールが出る要素を放置しない

---

## 5. 画像・メディア仕様

画像やメディアは、デバイス幅に応じて柔軟に変化することを前提とします。

---

### 5.1 推奨画像サイズ

| 用途 | 推奨サイズ | 備考 |
|---|---:|---|
| ヒーローバナー | 1920 × 1080px | 16:9。ビジュアル訴求が強い場合 |
| ヒーローバナー（横長） | 1920 × 600px | パノラマ型。ファーストビューで高さを抑えたい場合 |
| スライダー | 1280 × 500px | ファーストビューで次のコンテンツを見せるため、高さを抑える |

---

### 5.2 高解像度対応

Retinaディスプレイ向けに、表示サイズの **2倍（2x）〜3倍（3x）** の元画像を用意します。

| 表示サイズ | 推奨元画像 |
|---:|---:|
| 640px幅 | 1280px〜1920px幅 |
| 960px幅 | 1920px〜2880px幅 |

---

### 5.3 レスポンシブ挙動

画像に固定pxを指定することは避けます。  
親コンテナに対して柔軟に収まるようにします。

```css
img {
  max-width: 100%;
  height: auto;
}
```

#### ルール

- `width: 100%;`
- `height: auto;`
- 固定幅指定は避ける
- 画像比率が崩れないようにする
- 必要に応じて `object-fit: cover;` を使用する

---

## 6. アクセシビリティ・インタラクション基準

WCAG準拠を前提とし、すべてのユーザーが操作しやすいサイズ・色彩設計を行います。

---

### 6.1 タップターゲット（クリック領域）

| 種類 | サイズ |
|---|---:|
| 絶対下限値 | 24px × 24px |
| 推奨理想値 | 44px × 44px または 48px × 48px |

#### ルール

- WCAG 2.2 レベルAA基準では、24px × 24pxが下限
- 実務上は44px〜48px四方を推奨
- ファットフィンガー問題を回避する
- グラフィックが小さくても、透明なPaddingでクリック領域を拡張する

```css
.icon-button {
  width: 48px;
  height: 48px;
  padding: 12px;
}
```

---

### 6.2 コントラスト比

フォントサイズやウェイトが視認性に影響するため、以下の比率を厳守します。

| 対象 | 必要なコントラスト比 |
|---|---:|
| 通常テキスト（18px未満） | 4.5 : 1 以上 |
| 大きなテキスト（18px以上、または14px以上の太字） | 3 : 1 以上 |

#### 注意点

- 薄いブランドカラーを白背景の文字に使わない
- プレースホルダーを薄くしすぎない
- 重要情報を低コントラストで表示しない
- 背景色と文字色の組み合わせは必ず確認する

---

## 7. 実装時の基本方針

### 7.1 サイズ設計の原則

- 余白は8pxグリッドを基本にする
- 本文サイズは16px未満にしない
- 日本語本文の行間は1.7〜1.9を基準にする
- 主要CTAは高さ48pxを基本にする
- テキスト記事の最大幅は640px〜760pxに制限する
- モバイル320px幅で崩れないようにする
- レスポンシブ設計はモバイルファーストで行う
- PC用CSSをスマホで打ち消す設計は避ける

---

### 7.2 CSSトークン例

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
  --space-12: 48px;
  --space-16: 64px;

  --font-size-body: 16px;
  --font-size-h3: 20px;
  --font-size-h2: 24px;
  --font-size-h1-min: 32px;
  --font-size-h1-max: 48px;

  --line-height-body: 1.8;

  --container-wide: 1280px;
  --container-lp: 1140px;
  --container-article: 720px;
}
```

---

### 7.3 見出しサイズ例

```css
body {
  font-size: var(--font-size-body);
  line-height: var(--line-height-body);
  letter-spacing: 0.04em;
}

h1 {
  font-size: clamp(32px, 5vw, 48px);
  line-height: 1.3;
}

h2 {
  font-size: 24px;
  line-height: 1.5;
}

h3 {
  font-size: 20px;
  line-height: 1.6;
}

p {
  margin-bottom: 32px;
}
```

---

## 8. チェックリスト

### タイポグラフィ

- [ ] 本文サイズは16px以上になっている
- [ ] H2は本文より明確に大きい
- [ ] 日本語本文の行間は1.7〜1.9になっている
- [ ] 注釈テキストを重要情報に使っていない

### 余白

- [ ] 余白は8pxグリッドに沿っている
- [ ] カード一覧やリストには `gap` を使用している
- [ ] セクション間に十分な余白がある
- [ ] 段落間が詰まりすぎていない

### コンポーネント

- [ ] ボタンの高さは原則48pxになっている
- [ ] アイコンのクリック領域は44px〜48px程度ある
- [ ] カード内部とカード同士の余白が区別されている

### レイアウト

- [ ] 記事本文の幅は640px〜760px程度に収まっている
- [ ] 320px幅でも横スクロールが発生しない
- [ ] 768px / 1024px / 1280pxでレイアウト切り替えが適切に行われている
- [ ] モバイルでは1カラムを基本にしている
- [ ] PC用の横並びがモバイルで縦並びになっている
- [ ] サイドバーがモバイルでメインコンテンツの下に移動している

### 画像・表・埋め込み

- [ ] 画像は `max-width: 100%; height: auto;` で制御されている
- [ ] Retina対応用に2x〜3x画像を用意している
- [ ] ヒーロー画像やスライダー画像の高さが大きすぎない
- [ ] 表が画面幅を超える場合は横スクロール対応になっている
- [ ] iframeや動画がレスポンシブ対応になっている

### アクセシビリティ

- [ ] 通常テキストのコントラスト比は4.5:1以上
- [ ] 大きなテキストのコントラスト比は3:1以上
- [ ] タップターゲットは最低24px、推奨44px〜48px以上
- [ ] プレースホルダーや補足文が薄すぎない

### モバイル確認

- [ ] 320px幅で横スクロールが発生していない
- [ ] 本文サイズが16px以上になっている
- [ ] 主要ボタンの高さが48px以上ある
- [ ] アイコンボタンのタップ領域が44px以上ある
- [ ] カードやセクションの余白が詰まりすぎていない
- [ ] ハンバーガーメニューが操作しやすい
- [ ] テキストリンク同士が近づきすぎていない

---

## 9. まとめ

本設計書では、Webデザインにおけるサイズ・余白・レイアウト・画像・アクセシビリティ・モバイル対応の基準を定義しました。

特に重要な基準は以下です。

- 本文サイズは **16px以上**
- 日本語本文の行間は **1.7〜1.9**
- 余白は **8pxグリッド**
- ボタン高さは **48px基準**
- 記事本文幅は **640px〜760px**
- 最低対応幅は **320px**
- レスポンシブ設計は **モバイルファースト**
- タップ領域は **44px〜48px推奨**
- 通常テキストのコントラスト比は **4.5:1以上**

これらを共通ルールとして運用することで、デザインの統一感、読みやすさ、アクセシビリティ、保守性を高めることができます。