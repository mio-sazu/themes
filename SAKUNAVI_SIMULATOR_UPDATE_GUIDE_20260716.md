# 返済シミュレーター・会社データ表示 更新マニュアル（〜2026-07-16）

対象リポジトリ: `wp-content/themes/`（親テーマ `sakunavi-theme` ／ 子テーマ `sakunavi-child-migrated`）

返済シミュレーター・ローン診断・カードローン会社記事・会社一覧カードにまたがる「無利息期間」「実質年率」の表示調整と、いくつかの不具合修正をまとめた内容です。

---

## 1. 無利息期間の表示文言（`no_interest_label`）

### 背景
無利息期間は「無利息 30日間」のように日数だけを機械的に表示していましたが、「無利息初回最大30日間」「初めてなら最大30日間利息0円」のように、**適用条件を含めた正確な文言**で表示する必要が出てきたため対応。

### 管理画面での入力場所
会社編集画面 →「会社情報」タブ →「無利息期間（日）」のすぐ下に「無利息 表示文言（任意）」という欄があります。

### 表示ルール（重要）
- **文言欄に入力があれば、その文言をそのまま完結した文章として表示します（日数は自動で追記されません）。**
  - 例：文言欄に「初めてなら最大30日間利息0円」と入力 → そのまま「初めてなら最大30日間利息0円」と表示
  - 日数と文言を両方見せたい場合は、文言の中に自分で日数を含めて入力してください（例：「無利息初回最大30日間」）
- **文言欄が空欄の場合**は、今まで通り「無利息 ○日間」を自動表示します（「無利息期間（日）」の数値がそのまま使われます）。
- **「無利息期間（日）」の数値は、文言を入力した場合も返済シミュレーターの無利息計算（利息が0円になる月の判定など）に引き続き使われます。** 文言を入れたからといって日数欄を空にしないでください。空にすると、返済シミュレーターで無利息を適用するチェックボックス自体が出なくなります。

### 表示される場所（4箇所・すべて同じ文言が使われます）
1. 返済シミュレーター（会社選択カード・「その他の会社から選ぶ」ドロップダウン・上部バッジ・無利息チェックボックスのラベル）
2. カードローン簡易診断の結果カード
3. カードローン会社記事（基本情報テーブルの「無利息期間」行）
4. カードローン会社の一覧カード

> **運用メモ**：返済シミュレーターと会社記事とで文言を分けたい場面が今後増えてきた場合は、フィールドを2つに分ける対応も可能です。まずはこの1本の文言欄で運用し、必要になったタイミングで相談してください。

---

## 2. フリガナ検索対応（`name_kana`）

### 背景
返済シミュレーターの「その他の会社から選ぶ」検索で、ひらがな入力だと漢字・英語表記の社名（例：楽天銀行スーパーローン、SMBCモビット）がヒットしない問題への対応。

### 自動で効くもの（入力不要）
カタカナ社名（アコム・プロミス等）は、ひらがなで入力しても自動的にヒットします（「あこむ」→「アコム」がヒット）。英字社名も大文字・小文字を区別せず検索できます。

### 個別に入力が必要なもの
漢字・英語を含む社名で読みからも検索できるようにしたい場合のみ、会社編集画面の「会社情報」タブ →「フリガナ（任意）」に読みを入力してください（カタカナ・ひらがなどちらでも可）。空欄のままでも問題ありません。

### 触ったファイル
- `sakunavi-child-migrated/functions.php`（ACFフィールド追加）
- `sakunavi-theme/functions.php`（データ受け渡し）
- `sakunavi-theme/assets/js/simulator.js`（検索ロジック）

---

## 3. 実質年率の表示を入力どおりに統一

### 背景
実質年率（`rate_min`／`rate_max`）を内部で数値化して丸めていたため、「18.000」と入力しても「18」とだけ表示されたり、桁数がバラついたりしていました。

### 対応内容
表示専用にACFへ入力した文字列をそのまま使うように変更しました。**管理画面で入力した通りの桁数がそのまま表示されます。** 特別な操作は不要です（今まで通り「rate_min」「rate_max」に入力するだけ）。

- 実際の金利計算（返済シミュレーターの利率自動セットなど）は、これまで通り数値として内部処理されるため影響ありません。
- この対応は返済シミュレーターとローン診断のみが対象です。カードローン会社記事・一覧カードの金利表示は別のテキスト欄（`rate_prefix`など）を使った独立した仕組みのため、今回は対象外です。

---

## 4. 返済シミュレーターのタイトル統一

「カードローン返済シミュレーション」という表記を、ページ見出し・会社未選択時の表示・CSV保存時のタイトルの3箇所すべて「返済シミュレーター」に統一しました。

---

## 5. 会社検索ボックスの日本語入力（IME）不具合を修正

### 症状
「その他の会社から選ぶ（全◯社）」の検索欄で、日本語入力中に文字が抜けたり、変換が意図せず確定したりする不具合。

### 原因
検索欄は1文字入力するたびに、入力欄を含むDOM全体を作り直す実装になっていました。半角英数字なら都度フォーカスと文字位置を復元してごまかせますが、日本語IME（変換中）の途中で入力欄そのものを壊して再生成すると、ブラウザの変換セッションが強制的に切れてしまうことが原因でした。

### 対応内容
IME変換中は再描画を止め、変換確定のタイミングでまとめて検索結果を更新するように修正しました。管理画面での操作は不要です。

---

## 6. 返済シミュレーターの利息計算で小数点以下を保持

### 症状
毎月の利息額を整数（円未満切り捨てではなく四捨五入）に丸めてから合計していたため、利息合計・総返済額などの小数部分が反映されていませんでした。

### 対応内容
丸めの単位を「整数」から「小数点第2位まで」に変更。毎月の利息・元金充当額・残高・利息合計・総返済額・CSV出力のすべてに小数点以下が反映されるようになりました。借入額・毎月返済額など、そもそも整数でしか入力できない項目は今まで通り整数表示のままです。

---

## 触ったファイル一覧（今回の一連の対応）

| ファイル | 内容 |
|---|---|
| `sakunavi-child-migrated/functions.php` | ACFフィールド追加（無利息表示文言／フリガナ） |
| `sakunavi-theme/functions.php` | 会社データの組み立て（表示文言・フリガナ・金利ラベル）、ページ見出し |
| `sakunavi-theme/assets/js/simulator.js` | 表示文言・金利表示・検索・IME対応・利息の小数点表示 |
| `sakunavi-theme/assets/js/loan-diagnosis.js` | 診断結果カードの表示文言・金利表示 |
| `sakunavi-child-migrated/single-card_loan_company.php` | 会社記事の無利息期間表示 |
| `sakunavi-child-migrated/template-parts/loop-card.php` | 一覧カードの無利息期間表示 |

サーバーへは上記6ファイルすべてをアップロードしてください（`sakunavi-child-migrated/functions.php`にACFフィールド定義があるため、これが未アップロードだと他のファイルで文言・フリガナが反映されません）。
